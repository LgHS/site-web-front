framework:
    secret: DILIGENTHORSEPACKINGSTUFFUNICORNLOVE
    templating:
        engines: ['twig']
    profiler: { only_exceptions: false }
twig:
    globals:
        scheduler: '@scheduler'

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: notice
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: notice